# Решение проблем - Goida AI Unlocker для macOS

Руководство по решению распространённых проблем при использовании Goida AI Unlocker на macOS.

---

## 🚫 Проблемы с запуском

### Ошибка: "PySide6 не установлена"

**Причина:** Не установлена библиотека PySide6.

**Решение:**
```bash
pip install PySide6
```

Или установите все зависимости:
```bash
pip install -r requirements.txt
```

---

### Ошибка: "Python не найден"

**Причина:** Python 3 не установлен или не добавлен в PATH.

**Решение:**

1. Установите Python с официального сайта:
   - Перейдите на [python.org](https://www.python.org/downloads/)
   - Скачайте версию 3.8 или новее

2. Или установите через Homebrew:
```bash
brew install python3
```

---

### Окно не открывается / зависает при запуске

**Причина:** Возможные проблемы с Qt или графической системой.

**Решение:**

1. Переустановите PySide6:
```bash
pip uninstall PySide6
pip install PySide6 --upgrade
```

2. Запустите с выводом отладочной информации:
```bash
python main.py 2>&1 | tee debug.log
```

3. Проверьте системные требования:
   - macOS 10.15 (Catalina) или новее
   - Не запущен через SSH/удалённый доступ без X11

---

## 🔐 Проблемы с правами доступа

### "Не удалось установить файл hosts"

**Причина:** Недостаточно прав для изменения `/etc/hosts`.

**Решение:**

1. При запросе введите пароль администратора
2. Убедитесь, что ваш пользователь имеет права администратора
3. Проверьте права на файл:
```bash
ls -la /etc/hosts
```

Должно быть: `-rw-r--r--  1 root  wheel`

---

### Запрос пароля не появляется

**Причина:** AppleScript блокируется настройками безопасности.

**Решение:**

1. Откройте **Системные настройки** → **Защита и безопасность**
2. Разрешите Terminal.app (или вашему редактору кода) управлять компьютером
3. Перезапустите приложение

Альтернативный способ (ручной):
```bash
sudo python main.py
```

---

## 🌐 Проблемы с сетью

### "Не удалось проверить версию" / "Ошибка загрузки hosts"

**Причина:** Нет подключения к интернету или заблокирован доступ к GitHub.

**Решение:**

1. Проверьте подключение к интернету
2. Проверьте доступ к GitHub:
```bash
curl -I https://raw.githubusercontent.com
```

3. Если используете VPN/прокси, временно отключите его
4. Проверьте файрвол macOS:
   - **Системные настройки** → **Защита и безопасность** → **Файрвол**

---

### Сайты всё ещё заблокированы после установки

**Причина:** DNS кэш не обновился или используется сторонний DNS.

**Решение:**

1. Очистите DNS кэш вручную:
```bash
sudo dscacheutil -flushcache
sudo killall -HUP mDNSResponder
```

2. Перезапустите браузер (обязательно!)

3. Проверьте настройки DNS в **Системные настройки** → **Сеть**:
   - Убедитесь, что не используется сторонний DNS (1.1.1.1, 8.8.8.8)
   - Или оставьте пустым для автоматического получения

4. Если используется VPN:
   - Отключите VPN
   - Установите обход
   - Снова включите VPN (если необходимо)

---

## 🎨 Проблемы с интерфейсом

### Иконки не отображаются

**Причина:** Папка `icons` не найдена или повреждена.

**Решение:**

1. Убедитесь, что папка `icons` находится в той же директории, что и `main.py`
2. Проверьте содержимое:
```bash
ls icons/
```

Должны быть файлы: `settings.svg`, `trash.svg`, `sun.svg` и др.

3. Если папка отсутствует, скачайте её из репозитория

---

### Тема не меняется / не соответствует системной

**Причина:** Проблемы с чтением настроек macOS.

**Решение:**

1. Вручную переключите тему кнопкой в приложении
2. Проверьте системную тему:
```bash
defaults read -g AppleInterfaceStyle
```

Если ошибка - тема светлая, если выводит "Dark" - тёмная

---

## 📦 Проблемы со сборкой

### PyInstaller: "Module not found"

**Причина:** Не все модули включены в сборку.

**Решение:**

Используйте полную команду с явным указанием зависимостей:
```bash
pyinstaller \
    --name="Goida AI Unlocker" \
    --windowed \
    --onefile \
    --hidden-import=PySide6.QtSvg \
    --hidden-import=PySide6.QtWidgets \
    --icon=icon.ico \
    --add-data="icon.ico:." \
    --add-data="app_info.json:." \
    --add-data="icons:icons" \
    --clean \
    main.py
```

---

### "App is damaged and can't be opened"

**Причина:** Атрибут карантина macOS.

**Решение:**
```bash
xattr -cr "dist/Goida AI Unlocker.app"
```

Затем запустите приложение снова.

---

### Собранное приложение слишком большое

**Причина:** PyInstaller включает всё окружение Python.

**Решение:**

1. Используйте виртуальное окружение только с необходимыми пакетами:
```bash
python -m venv venv_build
source venv_build/bin/activate
pip install PySide6
pyinstaller ...
```

2. Используйте опцию `--exclude-module`:
```bash
--exclude-module=matplotlib \
--exclude-module=numpy \
--exclude-module=pandas
```

---

## 🔄 Проблемы с обновлением

### "Не удалось проверить обновления"

**Причина:** Файл `app_info.json` повреждён или недоступен.

**Решение:**

1. Проверьте наличие файла:
```bash
cat app_info.json
```

2. Восстановите содержимое:
```json
{
  "version": "1.0.0",
  "update_info_url": "https://raw.githubusercontent.com/AvenCores/Goida-AI-Unlocker/main/app_info.json",
  "download_url": "https://github.com/AvenCores/Goida-AI-Unlocker/releases/latest"
}
```

---

## 🐛 Отладка

### Включение подробного вывода

Запустите с выводом всех ошибок:
```bash
python -v main.py 2>&1 | tee debug.log
```

### Проверка файла hosts

Посмотреть текущее содержимое:
```bash
cat /etc/hosts
```

Восстановить оригинальный hosts вручную:
```bash
sudo nano /etc/hosts
```

Удалите все строки после стандартного содержимого.

### Сброс настроек

Удалите кэш приложения:
```bash
rm -rf ~/Library/Caches/Goida*
rm -rf __pycache__
```

---

## 📞 Получение помощи

Если проблема не решена:

1. 📝 Создайте issue на GitHub с описанием:
   - Версия macOS
   - Версия Python
   - Полный текст ошибки
   - Лог файл (`debug.log`)

2. 💬 Опишите шаги для воспроизведения проблемы

3. 📸 Приложите скриншот ошибки (если есть)

---

**Помните:** Изменение файла `/etc/hosts` требует осторожности. Всегда можно восстановить оригинальный файл кнопкой "Удалить обход блокировок".

